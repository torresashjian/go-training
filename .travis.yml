language: go
sudo: false
go:
  - 1.8.x
jobs:
  include:
    - stage: test
      script: go test -v ./...
    - stage: install
      before_install:
            - "echo Hello"
      script: go install ./...